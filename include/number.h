#ifndef NUMBER_H
#define NUMBER_H

#include <string>

using std::string;

class Number {
    public:
        Number () {}
        string symbol() const { return _speed; }
        string value() const { return _speed; }
        int match() const { return _speed; }
};

#endif
